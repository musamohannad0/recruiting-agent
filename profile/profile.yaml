# Synthetic fallback used only before a private workspace is created.
candidate:
  name: Example Candidate

target_roles:
  - Product Management

preferences:
  locations: [Remote (US)]
  notes: This example must not be used as an activated candidate profile.

hard_filters:
  exclude_role_types: []
