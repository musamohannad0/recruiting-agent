# Anthropic (used by claude-agent-sdk; optional if `claude` CLI is already authenticated)
ANTHROPIC_API_KEY=

# Langfuse (optional — tracing is skipped when unset)
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://us.cloud.langfuse.com
